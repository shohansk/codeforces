#include <bits/stdc++.h>
using namespace std;
int main ()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    string k;
    int a,n,c=0;
    cin>>n>>a;
    cout<<n;
    for(int i=0;i<n;i++)
       {

           cin>>k[i];
           if(k[i]<a)
           {
               c++;
               cout<<k[i];
           }
           cout<<"shohan"<<endl;
       }
       cout<<c<<endl;
}
